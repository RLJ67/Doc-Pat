<?php
// 1. Logic: Handle Search Input
$search_query = isset($_GET['query']) ? urlencode($_GET['query']) : '';
$results = [];

if ($search_query) {
    $api_url = "https://api.jikan.moe/v4/anime?q={$search_query}&limit=20";
    
    $ch = curl_init();
    curl_setopt($ch, CURLOPT_URL, $api_url);
    curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
    curl_setopt($ch, CURLOPT_FOLLOWLOCATION, true);
    $response = curl_exec($ch);
    $http_code = curl_getinfo($ch, CURLINFO_HTTP_CODE);
    curl_close($ch);

    if ($http_code === 200) {
        $data = json_decode($response, true);
        $results = $data['data'] ?? [];
    }
}
?>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AnimeVault | Jikan API Explorer</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #0f172a; color: #f8fafc; }
    </style>
</head>
<body class="p-6 md:p-12">

    <div class="max-w-6xl mx-auto text-center mb-12">
        <h1 class="text-4xl font-bold mb-4 text-sky-400">AnimeVault</h1>
        <p class="text-slate-400">Discover and track your favorite series using the Jikan API.</p>
        
        <form action="" method="GET" class="mt-8 flex justify-center">
            <div class="relative w-full max-w-md">
                <input type="text" name="query" placeholder="Search anime... (e.g. One Piece)" 
                       class="w-full px-6 py-3 rounded-full bg-slate-800 border border-slate-700 focus:outline-none focus:ring-2 focus:ring-sky-500 text-white"
                       value="<?php echo htmlspecialchars($_GET['query'] ?? ''); ?>" required>
                <button type="submit" class="absolute right-2 top-1.5 bg-sky-500 hover:bg-sky-600 px-4 py-1.5 rounded-full text-sm font-semibold transition">
                    Search
                </button>
            </div>
        </form>
    </div>

    <div class="max-w-6xl mx-auto">
        <?php if ($search_query && empty($results)): ?>
            <div class="text-center py-20">
                <p class="text-slate-500 text-lg">No results found for "<?php echo htmlspecialchars($_GET['query']); ?>".</p>
            </div>
        <?php endif; ?>

        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8">
            <?php foreach ($results as $anime): ?>
                <div class="bg-slate-800 rounded-xl overflow-hidden shadow-lg hover:scale-105 transition-transform duration-300 flex flex-col">
                    <div class="relative h-72 overflow-hidden">
                        <img src="<?php echo $anime['images']['webp']['large_image_url']; ?>" 
                             class="w-full h-full object-cover" alt="Cover">
                        <div class="absolute top-2 right-2 bg-black/70 px-2 py-1 rounded text-xs font-bold text-yellow-400">
                            ★ <?php echo $anime['score'] ?? 'N/A'; ?>
                        </div>
                    </div>

                    <div class="p-4 flex-grow">
                        <h3 class="font-bold text-lg leading-tight mb-2 line-clamp-2">
                            <?php echo $anime['title']; ?>
                        </h3>
                        <div class="flex flex-wrap gap-2 mb-3">
                            <span class="text-[10px] uppercase tracking-wider font-bold bg-slate-700 px-2 py-0.5 rounded text-slate-300">
                                <?php echo $anime['type']; ?>
                            </span>
                            <span class="text-[10px] uppercase tracking-wider font-bold bg-sky-900/50 px-2 py-0.5 rounded text-sky-300">
                                <?php echo $anime['status']; ?>
                            </span>
                        </div>
                        <p class="text-slate-400 text-sm line-clamp-3 mb-4">
                            <?php echo $anime['synopsis']; ?>
                        </p>
                    </div>

                    <div class="p-4 border-t border-slate-700 bg-slate-800/50">
                        <a href="<?php echo $anime['url']; ?>" target="_blank" 
                           class="block text-center text-sm font-bold text-sky-400 hover:text-sky-300 transition">
                            View on MAL →
                        </a>
                    </div>
                </div>
            <?php endforeach; ?>
        </div>
    </div>

</body>
</html>
